var dbname = 'blog';
var username = 'root';
var password = '';
var host = 'localhost';
var port = 3306;

var config = {'database':{
    dbname:dbname,
    username:username,
    password:password,
    host:host,port:port}
};

module.exports = config;